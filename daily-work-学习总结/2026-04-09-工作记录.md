# 2026-04-09 工作记录

## 今日内容

- 根据 [`docs/100-day-plan/handbooks/100天服务器系统工程强化训练手册_HardMode.xlsx`](/C:/Users/王文超/Desktop/Servre_SIT_Mastery/docs/100-day-plan/handbooks/100天服务器系统工程强化训练手册_HardMode.xlsx) 定位并确认 `Day19` 对应主题为 `PCIe自动检测脚本`。
- 新增 [`daily-work-学习总结/Day19-PCIe自动检测脚本.md`](/C:/Users/王文超/Desktop/Servre_SIT_Mastery/daily-work-学习总结/Day19-PCIe自动检测脚本.md)，把 Day19 的学习目标、脚本思路、易错点和自测题整理成正式学习稿。
- 新增 [`practice/scripts-练手脚本/check_pcie.py`](/C:/Users/王文超/Desktop/Servre_SIT_Mastery/practice/scripts-练手脚本/check_pcie.py)，实现基于 `lspci` 的 `NVMe / NIC / NPU` 分类、计数、阈值校验与汇总输出。
- 更新 [`daily-work-学习总结/Day学习索引.md`](/C:/Users/王文超/Desktop/Servre_SIT_Mastery/daily-work-学习总结/Day学习索引.md)，把学习入口推进到 `Day19`。
- 更新 [`practice/scripts-练手脚本/README.md`](/C:/Users/王文超/Desktop/Servre_SIT_Mastery/practice/scripts-练手脚本/README.md)，补充 `check_pcie.py` 和 `nvme_hotplug_scripts/` 入口。
- 将根目录的 `nvme_hotplug_scripts/` 收口到 [`practice/scripts-练手脚本/nvme_hotplug_scripts`](/C:/Users/王文超/Desktop/Servre_SIT_Mastery/practice/scripts-练手脚本/nvme_hotplug_scripts)，把 `NVMe` 热插拔脚本与三份手册归档到练手脚本目录。
- 补充 [`practice/scripts-练手脚本/nvme_hotplug_scripts/run-hotplug-test.sh`](/C:/Users/王文超/Desktop/Servre_SIT_Mastery/practice/scripts-练手脚本/nvme_hotplug_scripts/run-hotplug-test.sh) 作为统一执行入口。
- 新增本日工作记录，并准备统一提交到 Git。
- 在本机完成 Claude Code 安装排查，确认 `npm` 环境里存在代理与网络波动；最终验证 `claude --version` 可正常输出 `2.1.97 (Claude Code)`。
- 针对 `10.121.176.138` 的 BMC 告警，结合仓库既有 `bmc-cases` 记录复核，确认当前根因方向仍指向 `CPU2 UBC5/UBC6` 不支持单元、8 个风扇批量告警和 `FLEXIO1_POWER` 电源告警，偏向扩展部件/线缆/槽位适配异常，而不是 BMC 自身故障。

## 今日问题

### 问题1：Day19 只有计划，没有落地学习稿

- 现象：仓库里已经有 `Day13` 到 `Day18`，但没有 `Day19` 正文。
- 影响：学习链路在 `NUMA` 之后中断，无法继续顺着计划推进。
- 初步判断：需要先从训练手册中定位 Day19 主题，再按既有 Day 文档风格补齐正文和脚本。

### 问题2：`check_pcie.py` 需要从“人工看设备”升级到“自动清点设备”

- 现象：`lspci` 可以看见设备，但手工逐行数 `NVMe / NIC / NPU` 效率低，也不方便和目标配置核对。
- 影响：现场巡检和复盘都缺少可重复执行的基础脚本。
- 初步判断：第一版脚本只要完成 `lspci -> 分类 -> 计数 -> 阈值检查 -> 总结输出` 就已经有工程价值。

### 问题3：今天的 `NVMe` 热插拔资料散落在仓库根目录

- 现象：`nvme_hotplug_scripts/` 直接放在仓库根目录，不符合现有 `practice/scripts-练手脚本/` 的收口方式。
- 影响：后续目录会继续变乱，也不方便统一索引。
- 初步判断：应把这批现场脚本和手册归档到练手脚本目录，再由 README 统一暴露入口。

### 问题4：Claude Code 安装过程被网络状态干扰

- 现象：多次 `npm install -g @anthropic-ai/claude-code` 出现超时或 `ECONNRESET`，一度只留下启动器而没有完整包体。
- 影响：表面上看命令已存在，但 `claude --version` 实际会报 `MODULE_NOT_FOUND`。
- 初步判断：本机代理配置存在，且安装过程受网络波动影响；清理残留后继续重试，最终包体落盘并可正常启动。

### 问题5：BMC 告警需要结合既有案例复核

- 现象：今天再次查看 `10.121.176.138` 的 BMC 告警时，仓库内已有现成案例和 Redfish 抓取记录可复用，但当前会话没有可直接使用的 BMC 凭据，无法对设备做一次新的在线抓取。
- 影响：不能把“今天的 BMC 请求”误写成已经完成了实时采集。
- 初步判断：应以现成案例为主线复核，结论继续维持在扩展部件/线缆/槽位适配异常方向。

## 关键命令/记录

```text
# 定位 Day19
读取 docs/100-day-plan/handbooks/100天服务器系统工程强化训练手册_HardMode.xlsx
确认 01-100天计划 工作表中的 Day19 行

# 新增 Day19 学习稿
编辑 daily-work-学习总结/Day19-PCIe自动检测脚本.md
编辑 daily-work-学习总结/Day学习索引.md

# 新增 PCIe 自动检测脚本
编辑 practice/scripts-练手脚本/check_pcie.py
python -m py_compile practice/scripts-练手脚本/check_pcie.py

# 归档今天的 NVMe 热插拔资料
移动 nvme_hotplug_scripts -> practice/scripts-练手脚本/nvme_hotplug_scripts
更新 practice/scripts-练手脚本/README.md

# Claude Code 安装与验证
npm install -g @anthropic-ai/claude-code
irm https://claude.ai/install.ps1 | iex
claude --version

# BMC 告警复核
阅读 docs/bmc-cases/2026-03-31-10.121.176.138-bmc-alarm-analysis.md
阅读 daily-work-学习总结/2026-03-30-工作复盘.md
复核 10.121.176.138 告警根因方向
```

## 分析过程

1. 先确认 `Day19` 是否已经存在于仓库，结果发现只有索引提到后续会追加，正文尚未落盘。
2. 从 `HardMode.xlsx` 中直接定位 `Day19` 行，确认主题是 `PCIe自动检测脚本`，并抽取出“学习重点、命令/工具、实验任务、硬性产出”四个核心字段。
3. 按 `Day17`、`Day18` 的文档风格补出 `Day19`，保证阅读节奏仍然是“目标 -> 原理 -> 方法 -> 易错点 -> 结论”。
4. 以最小可用版本实现 `check_pcie.py`，先完成 `NVMe / NIC / NPU` 三类统计与预期值比对，而不是一开始就把脚本写得过重。
5. 用 `python -m py_compile` 做语法校验，确保脚本至少在当前 Windows Python 环境下可被解释。
6. 盘点仓库新增内容时，发现 `nvme_hotplug_scripts/` 直接放在根目录，因此把它归入 `practice/scripts-练手脚本/`，让现场脚本也走统一入口。
7. 对 Claude Code 安装问题不做表面判断，而是把“启动器是否存在”和“包体是否完整”分开验证，最终确认本机已经达到可用状态。
8. 复核 `10.121.176.138` 的 BMC 告警分析时，优先使用仓库内已整理好的 Redfish / BMC 案例，而不是假设本次已经重新在线抓取成功。

## 今日结论

- `Day19` 已经从计划表正式落成学习稿，学习链路从 `Day18` 顺利推进到 `Day19`。
- `check_pcie.py` 已经具备基础工程价值，可以一键统计 `NVMe / NIC / NPU` 并输出阈值检查结果。
- 今天新增的 `NVMe` 热插拔脚本和手册已经从仓库根目录收口到 `practice/scripts-练手脚本/`，目录结构更清晰。
- `run-hotplug-test.sh` 已经成为 NVMe 热插拔统一执行入口。
- Claude Code 已在本机达到可运行状态，后续可直接使用 `claude` 命令。
- `10.121.176.138` 的 BMC 告警方向已复核，当前仍应按照扩展部件/线缆/槽位适配异常方向继续追。

## 后续动作

- 下一步可以把 `check_pcie.py` 升级为 `v2`，补充 `BDF 地址`、`NUMA 节点`、`lspci -vv` 链路速率和宽度检查。
- `nvme_hotplug_scripts` 后续如果形成稳定方法论，可以把手册进一步沉淀到 `docs/manual/`。
- 如果后续还会频繁安装 CLI 工具，建议单独记录本机代理和 `npm` 网络状态，避免再次出现半安装残留。
- 如果后续要继续抓 BMC 实时日志，先补齐凭据和访问方式，再做 Redfish 在线采集，避免把已有案例复述成新的抓取结果。

## 可沉淀到手册的点

- 如何把 `lspci` 的人工检查升级为可复用的 `PCIe` 自动清点脚本。
- 现场脚本目录如何做“主用脚本 / 专题脚本 / 历史脚本”分层管理。
- Windows 下 CLI 工具安装失败时，应区分“启动器落盘”和“实际包体可运行”两个层面。
